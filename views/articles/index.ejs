<h1>Latest headlines</h1>

<a href="/articles/new" class="btn btn-primary">Add</a>

<div class="container">
  <div class="row">
    <% articles.forEach((article) => { %>
    <div class="col-md-12">
      <img src="<%= article.images %>">
      <h2><%= article.name %></h2>
      <p><%= article.content %></p>
      </br>
      <p>Added by <%= article.comments.body %></p>
      <a href="/articles/<%= article.id %>/edit" class="btn btn-primary">Edit</a>
            <a href="/articles/<%= article.id %>/" class="btn btn-primary">Show</a>
      <form method="POST" action="/articles/<%= article.id %>">
        <input type="hidden" name="_method" value="DELETE">
        <button class="btn btn-danger">Delete</button>
      </form>

    </div>
  </div>
  <% }) %>
</div>
<%#
  // $(`<img src="${data2.articles[i].urlToImage}"><h2 class="head" >${data2.articles[i].title}</h2><p>${data2.articles[i].description}</p><h1 hidden>${data2.articles[i].url}</h1>`).appendTo($(`#${i}`));%>
